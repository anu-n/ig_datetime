[build]
  functions = "functions"
  command = "pip install -r requirements.txt && gunicorn --bind 0.0.0.0:8080 functions.app:app"
  timeout = 1200

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/app"
  status = 200
